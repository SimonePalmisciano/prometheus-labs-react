import styles from './DailysupBanner.module.css';

export default function DailysupBanner() {
    return (
        <section className={`${styles.dailysupBanner} ${styles.banner}`}>
        </section>
    );
}

